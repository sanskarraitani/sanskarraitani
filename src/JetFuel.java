import city.cs.engine.Shape;
import city.cs.engine.World;

public class JetFuel extends StaticEntities{

    JetFuel(World w, Shape s, int si, int x, int y, String img) {
        super(w, s, si, x, y, img);
    }
}
