import city.cs.engine.Shape;
import city.cs.engine.World;

public class Coins extends StaticEntities{

    Coins(World w, Shape s, int si, int x, int y, String img) {
        super(w, s, si, x, y, img);
    }

}
